---
version: alpha
name: 每日情报 UI/UX 交互规格
description: 基于当前「每日情报」桌面端 UI 反推出的交互、状态与用户路径规格。本文不评价视觉好坏，不新增产品模块，不改变现有页面布局。
source:
  screenshot: "/Users/zhuzijian/.codex/generated_images/019ff1ac-19a8-7672-9102-9e5559bdfbe1/exec-dc0babf4-287c-4cbf-9e86-54e935e8eb52.png"
  reference_date: "2026-08-13"
---

# 每日情报 UI/UX 交互规格

## 1. 目标与范围

### 1.1 用户目标

用户打开「每日情报」后，应能快速完成以下任务：

1. 了解今天最值得关注的行业趋势；
2. 按小红书、抖音、视频号查看平台来源；
3. 查看重点内容的封面、标题、摘要和互动数据；
4. 审核当前待发布的整份摘要；
5. 在不离开当前工作流的情况下进入趋势、内容或审核页面继续处理。

### 1.2 设计边界

- 当前页面是桌面端「每日总览」首页。
- 页面只承担“发现、判断、进入处理”的职责。
- 详情编辑、整份摘要编辑、账号设置等操作进入现有导航对应页面，不在首页新增独立模块。
- 不新增图表、平台 Logo、收藏面板、平台健康面板、推送日历或额外统计模块。
- 小红书、抖音、视频号在界面中只以文字展示，不使用平台 Logo 或平台专属图标。

### 1.3 推断说明

当前截图是静态 UI，未展示弹窗、详情页、加载页和错误页。以下规格只补充完成任务所必需的交互状态，不改变首页的固定布局。

## 2. 信息架构

```text
每日情报
├── 今日总览      当前首页
├── 趋势发现      查看趋势条目与趋势详情
├── 审核中心      编辑、审核并发布整份摘要
└── 设置          关注词、账号与推送相关设置
```

### 2.1 全局区域

| 区域 | 作用 | 主要交互 |
|---|---|---|
| 左侧导航 | 在四个核心工作区之间切换 | 点击导航项进入对应页面 |
| 顶部搜索 | 搜索行业、关键词、内容或趋势 | 点击输入、提交搜索、查看结果 |
| 用户区 | 显示当前登录账号 | 点击头像或姓名打开账号菜单 |
| 主内容区 | 展示今日趋势、审核摘要和重点内容 | 筛选、查看、审核、进入详情 |

## 3. 首页主流程

### 3.1 默认进入流程

```text
打开每日情报
→ 加载今日数据
→ 默认展示今日趋势
→ 用户选择平台分类
→ 浏览趋势条目
→ 查看重点内容或进入详情
→ 如有待审核摘要，进入审核流程
```

### 3.2 首屏判断顺序

页面信息层级必须支持以下阅读顺序：

1. 先看「今日趋势」中的主题和热度变化；
2. 再看「待审核摘要」是否需要处理；
3. 最后通过「重点内容」查看原始内容证据。

数据刷新或页面重新进入时，不应改变这三个区域的相对位置。

## 4. 左侧导航交互

### 4.1 导航项

- 今日总览
- 趋势发现
- 审核中心
- 设置

### 4.2 行为规则

- 当前页面对应的导航项保持选中态。
- 点击当前已选中的导航项不重复加载，也不清空当前筛选。
- 点击其他导航项后进入目标页面，并保留用户最近一次的筛选条件和搜索词，除非目标页面不支持该条件。
- 页面切换不应改变已完成的审核状态、收藏状态或用户标记。
- 浏览器返回操作应回到上一个页面及其筛选状态，而不是回到首页初始状态。

### 4.3 导航状态

| 状态 | 规则 |
|---|---|
| 默认 | 普通文字与低对比度图标；不抢占主内容注意力 |
| 选中 | 使用当前 UI 的紫色高亮背景和文字；表示当前工作区 |
| 不可用 | 仅在页面加载或权限不足时出现；不应伪装成可点击项 |

## 5. 顶部搜索交互

### 5.1 搜索范围

搜索框占位提示为“搜索行业、关键词、内容或趋势”。搜索范围至少包括：

- 行业关键词；
- 趋势标题；
- 内容标题；
- 内容摘要；
- 作者名称。

### 5.2 交互流程

```text
点击搜索框
→ 输入关键词
→ 提交搜索
→ 显示匹配结果
→ 点击结果进入对应趋势或内容
```

### 5.3 规则

- 输入为空时提交不产生跳转，也不改变当前首页内容。
- 搜索过程中显示明确的加载状态，避免用户重复提交。
- 无匹配时显示“未找到相关内容”，同时保留搜索词，便于用户修改。
- 搜索失败时保留用户已输入的文字，并提供重试，不返回空白页面。
- 从搜索结果进入趋势或内容详情后，返回时恢复原搜索词和结果位置。

## 6. 顶部摘要卡片交互

当前首页的三张摘要卡片为：

- 新增趋势：12
- 待审核：6
- 今日已推送：2/3

### 6.1 行为定义

| 卡片 | 点击后的目标 | 交互目的 |
|---|---|---|
| 新增趋势 | 趋势发现 | 快速查看今日新增趋势 |
| 待审核 | 审核中心 | 快速处理待审核摘要 |
| 今日已推送 | 审核中心的推送状态或当前摘要记录 | 确认当天推送进度 |

### 6.2 规则

- 摘要卡片整体可点击，不要求用户精准点击数字。
- 卡片数字是状态摘要，不直接作为独立按钮。
- 数字更新时只更新数字和相关状态，不自动把用户正在看的列表滚动到顶部。
- 无新增趋势时显示 0，不隐藏卡片。
- 无待审核摘要时显示 0，并将审核入口置为无待处理状态。

## 7. 「今日趋势」交互

### 7.1 区域职责

该区域用于帮助用户快速判断行业变化，不承担摘要编辑或原文阅读的完整任务。

每行趋势至少包含：

- 排名；
- 趋势主题；
- 一句话解读；
- 来源平台；
- 热度变化。

### 7.2 平台分类按钮

现有平台分类按钮：

- 小红书
- 抖音
- 视频号

#### 行为

- 任一时间只能有一个平台按钮处于选中态。
- 点击平台按钮后，只更新「今日趋势」区域内的趋势列表，不刷新整个首页。
- 选中态使用当前 UI 的紫色；未选中态保持深色面板中的中性文字。
- 用户切换平台后，保留当前页面滚动位置和其他区域状态。
- 首次进入时使用设计稿中的默认选中项；后续进入可恢复用户最近一次选择。
- 若所选平台没有数据，在趋势区域内显示空状态，不影响审核卡片和重点内容。

### 7.3 趋势行交互

- 点击趋势行的任意非空白区域，进入对应趋势详情或趋势发现页面。
- 点击行为以整行为点击区域，不要求点击标题文字。
- 进入详情时携带趋势主题、来源平台和当前时间范围上下文。
- 返回后恢复平台选中态、列表滚动位置和原先排序。
- 热度变化为只读信息，不作为独立按钮。
- 排名数字为展示信息，不可点击，不触发筛选。

### 7.4 趋势状态

| 状态 | 展示与行为 |
|---|---|
| 加载中 | 保留区域标题和平台按钮，列表行显示骨架占位；禁止重复触发同一筛选 |
| 有数据 | 显示排序后的趋势列表；默认按当前产品排序规则排列 |
| 无数据 | 在列表区域显示“该平台暂无新增趋势”，保留平台按钮 |
| 部分数据 | 正常显示可用数据；缺失平台不阻塞当前列表展示 |
| 加载失败 | 显示失败原因或“暂时无法加载”，提供重试；不清空已显示的旧数据 |

## 8. 「待审核摘要」交互

### 8.1 区域职责

右侧区域代表当前待审核的整份摘要。大图是摘要或重点内容的预览，不代表只审核这一条内容。

### 8.2 审核前流程

```text
查看待审核摘要预览
→ 点击预览区域或标题进入审核中心
→ 在审核中心编辑摘要、删除不相关内容、调整趋势顺序
→ 返回审核预览
→ 点击“审核通过”
→ 确认本次发布
→ 状态变为“已审核 / 已推送”
```

### 8.3 「审核通过」按钮

- 按钮是当前页面的唯一高风险主操作，使用橙色强调。
- 点击后不能静默完成发布；必须展示一次确认状态，明确这是对整份摘要的审核通过与推送操作。
- 确认前允许取消，取消后回到原待审核状态。
- 确认后按钮进入提交中状态，禁止重复点击。
- 成功后：
  - 按钮变为不可重复提交的已完成状态；
  - 待审核数量减少；
  - 今日已推送数量更新；
  - 当前预览标记为已审核。
- 失败后：
  - 保留待审核状态；
  - 恢复按钮可用；
  - 显示失败原因和重试入口；
  - 不得显示“已推送”等成功文案。

### 8.4 审核权限

- 编辑者可以进入审核中心修改摘要，但不能完成最终发布。
- 固定最终审核发布者可以执行“审核通过”。
- 固定发布者不可用时，备用发布者获得同等发布操作。
- 无发布权限的用户点击审核操作时，应看到权限提示，而不是无反馈或提交失败。

### 8.5 无待审核状态

若当前没有待审核摘要，区域仍保留原位置，显示“暂无待审核摘要”，主按钮置为禁用或不显示，不改变首页布局。

## 9. 「重点内容」交互

### 9.1 内容行信息结构

每行内容固定为三部分：

1. 左侧：封面；
2. 中间：主标题、副标题 / 摘要、来源平台、发布时间和标签；
3. 右侧：点赞、收藏、评论数量。

### 9.2 行为规则

- 整行可点击，点击后进入内容详情或原文跳转前的内容查看状态。
- 封面、主标题和副标题使用同一打开目标，避免用户判断不同元素的作用。
- 点赞、收藏、评论数为只读数据，不把数字做成独立操作按钮。
- 标签为内容分类信息，不可直接删除或编辑。
- 从内容详情返回后，恢复首页滚动位置，不重新排序内容列表。
- 原文跳转应在新标签页或受控外部页面打开，避免用户丢失当前情报浏览进度。

### 9.3 内容详情最小信息

进入内容详情后，至少保留当前列表已有信息，并补充产品需求中已确定的字段：

- 标题；
- 图片封面；
- 发布时间；
- 作者；
- 内容摘要；
- 点赞数；
- 收藏数；
- 部分评论；
- 原文链接。

### 9.4 内容状态

| 状态 | 展示与行为 |
|---|---|
| 封面加载中 | 显示固定尺寸占位，不能因图片加载改变行高 |
| 封面加载失败 | 使用中性占位，不影响标题和数据查看 |
| 内容已删除或不可访问 | 保留已采集摘要和来源信息，原文按钮置为不可用并说明原因 |
| 数据过期 | 保留最近一次数据，并标识数据时间；不伪装为实时数据 |

## 10. 账号与通知交互

### 10.1 通知入口

- 顶部通知图标显示未处理通知数量。
- 点击后进入通知列表或审核中心，不在首页展开复杂面板。
- 通知应按优先级优先展示：待审核、审核失败、采集失败、飞书同步失败。
- 阅读通知不会自动完成审核或推送操作。

### 10.2 用户菜单

- 点击用户头像或姓名打开当前账号菜单。
- 菜单只承载现有账号相关操作，例如账号信息、退出登录和权限信息。
- 退出登录前需要确认，避免误触导致当前审核工作丢失。

## 11. 数据更新与一致性

### 11.1 首页更新

- 页面首次打开时加载最新可用数据。
- 数据更新时更新数字、趋势列表和审核状态，但不打断用户当前正在进行的点击或审核确认。
- 用户正在查看某一趋势或内容时，不自动替换其详情内容。
- 若审核状态在其他账号处发生变化，当前页面应提示状态已更新，并要求用户重新确认，而不是继续提交旧版本。

### 11.2 部分平台采集失败

- 某个平台采集失败时，其他平台的可用数据仍然展示。
- 失败平台在对应来源位置显示不可用或未更新状态。
- 失败不应阻塞审核其他平台已形成的摘要。
- 平台恢复后，补采结果进入下一次数据更新；不强行改变用户已完成审核的历史版本。

### 11.3 审核版本一致性

- 用户点击“审核通过”时，提交的必须是当前看到的摘要版本。
- 若摘要在用户打开后被其他审核者修改，提交前提示“内容已更新，请重新查看”。
- 已发布摘要不可被同一次审核操作重复发布。

## 12. 通用反馈状态

### 12.1 加载

- 使用与卡片结构一致的骨架状态，不使用全屏阻塞式加载。
- 搜索、平台筛选、审核提交分别独立显示加载状态。
- 加载时保留标题、按钮位置和可返回路径。

### 12.2 空状态

空状态必须说明“当前没有什么”，并提供与当前任务相关的下一步：

- 今日趋势为空：提示暂无新增趋势；
- 待审核为空：提示暂无待审核摘要；
- 重点内容为空：提示暂无可展示内容；
- 搜索无结果：提示未找到相关内容并保留搜索词。

不因空状态删除卡片或重新排列首页布局。

### 12.3 错误

- 错误信息放在发生错误的区域内，避免用户不知道哪个操作失败。
- 失败后保留已有数据，不以空白覆盖旧数据。
- 可重试操作使用原按钮或紧邻的文字重试入口。
- 错误文案说明影响范围，例如“抖音数据暂未更新”，不要只显示“系统错误”。

## 13. 键盘与可访问性

- 左侧导航、平台按钮、趋势行、审核按钮、重点内容行均必须可通过键盘聚焦。
- Tab 顺序按视觉阅读顺序：顶部搜索 → 左侧导航 → 摘要卡片 → 今日趋势 → 待审核摘要 → 重点内容。
- 当前焦点必须有清晰的紫色焦点环，不能只依赖颜色变化。
- 平台按钮必须有选中状态的文本或语义标识，不能只依赖背景色。
- 审核通过按钮需要明确的可访问名称，包含“审核通过并推送当前摘要”的语义。
- 互动数据使用文本标签“点赞、收藏、评论”，不能只使用图标或数字。
- 文本、按钮和深色背景之间保持足够对比度；弱化文字不得用于承载关键操作。
- 动态更新的待审核数量和推送状态应向辅助技术暴露状态变化，但不强制抢夺焦点。

## 14. 关键任务验收标准

### 任务 A：查看某个平台的趋势

1. 用户进入首页；
2. 看到当前平台选中态；
3. 点击小红书、抖音或视频号；
4. 只有今日趋势列表更新；
5. 用户点击趋势行进入趋势详情；
6. 返回首页后平台选择和列表位置保持不变。

### 任务 B：审核并推送摘要

1. 用户看到待审核摘要预览；
2. 点击预览进入审核中心；
3. 编辑者可修改整份摘要，但不能发布；
4. 发布者点击审核通过；
5. 系统要求确认本次整份摘要的发布；
6. 提交期间按钮不可重复点击；
7. 成功后首页显示已审核 / 已推送状态；
8. 失败后保留待审核状态并支持重试。

### 任务 C：查看并打开重点内容

1. 用户在重点内容中扫描封面、标题和摘要；
2. 通过点赞、收藏、评论判断内容关注度；
3. 点击整行进入内容详情；
4. 在详情中查看部分评论和原文链接；
5. 点击原文后保留当前情报页面状态。

## 15. 不在本规格内的内容

- 不定义后端采集实现、爬虫方案或平台合规方案；
- 不定义微信小程序独立页面布局；
- 不新增客户注册、多租户或对外销售流程；
- 不新增趋势算法、热度计算公式或推荐模型；
- 不把点赞、收藏、评论设计为平台互动操作；
- 不在首页增加复杂图表、收藏面板、推送日历或平台健康面板。

## 16. 实现前必须确认的产品规则

以下事项会影响交互实现，但无法仅凭当前静态 UI 得出最终答案：

- 审核通过是否立即触发三类推送，还是只改变审核状态后等待定时推送；
- 平台分类按钮默认选中哪个平台，是否需要增加“全部”视图；
- 点击内容行是先进入站内详情，还是直接打开原文；
- 今日已推送卡片的目标页面是审核中心还是历史归档；
- 审核失败、内容失效和平台采集失败的最终文案。

这些问题不影响当前 UI 结构，但在进入技术核查前需要锁定。
